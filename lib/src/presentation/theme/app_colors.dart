import 'package:flutter/material.dart';

/// Sistema de colores profesional para trading de criptomonedas
/// Inspirado en plataformas de trading profesionales con modo oscuro
class AppColors {
  // Fondos oscuros estilo trading profesional
  static const Color darkBackground = Color(0xFF0D1117);
  static const Color cardBackground = Color(0xFF161B22);
  static const Color surfaceColor = Color(0xFF21262D);
  static const Color borderColor = Color(0xFF30363D);
  static const Color dividerColor = Color(0xFF21262D);

  // Textos
  static const Color primaryText = Color(0xFFF0F6FC);
  static const Color secondaryText = Color(0xFF8B949E);
  static const Color tertiaryText = Color(0xFF6E7681);
  static const Color disabledText = Color(0xFF484F58);

  // Colores de trading
  static const Color bullish = Color(0xFF26A641); // Verde profesional
  static const Color bearish = Color(0xFFF85149); // Rojo profesional
  static const Color neutral = Color(0xFF58A6FF); // Azul neutro
  static const Color opportunity = Color(0xFFFFD700); // Dorado
  static const Color alert = Color(0xFFFF9500); // Naranja
  static const Color warning = Color(0xFFFB8500); // Naranja intenso

  // Acentos
  static const Color primaryAccent = Color(0xFF58A6FF);
  static const Color secondaryAccent = Color(0xFF7C3AED);
  static const Color successColor = Color(0xFF26A641);
  static const Color infoColor = Color(0xFF0EA5E9);

  // Colores para gradientes
  static const Color gradientStart = Color(0xFF161B22);
  static const Color gradientEnd = Color(0xFF0D1117);

  // Colores para diferentes estados
  static const Color hoverColor = Color(0xFF30363D);
  static const Color selectedColor = Color(0xFF1F6FEB);
  static const Color disabledColor = Color(0xFF21262D);

  // Colores para badges y chips
  static const Color bullishLight = Color(0xFF26A641);
  static const Color bearishLight = Color(0xFFF85149);
  static const Color alertLight = Color(0xFFFF9500);

  // Colores para iconos
  static const Color iconPrimary = Color(0xFFF0F6FC);
  static const Color iconSecondary = Color(0xFF8B949E);
  static const Color iconTertiary = Color(0xFF6E7681);
}
